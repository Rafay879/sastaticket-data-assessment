# Stub - filled in by the next prompt.
# Will hold: the Glue Data Catalog database and crawler(s) that expose the
# gold-layer tables for querying. See DEPLOYMENT.md section 3.

variable "project_prefix" {
  type = string
}

variable "environment" {
  type = string
}
